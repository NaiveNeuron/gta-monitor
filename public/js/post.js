function Post(type, date, user, hostname, ip, level, command)
{
    this.type = type;
    this.date = date;
    this.user = user;
    this.hostname = hostname;
    this.ip = ip;
    this.command = command;
    this.level = level;
}
